#default arguments
def calculate_marks(maths, eng, hindi, comp, history = 34.56):# this is default value of a parameter it is not required to pass the history mark and default should be at last of the parameter list, there can be one mote default list
    

    print(f"")